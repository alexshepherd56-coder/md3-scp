[build]
  # Don't run any build command - this is a static site
  command = ""
  # Publish from the root directory
  publish = "."

# Redirect all routes to index.html for SPA behavior (optional)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
